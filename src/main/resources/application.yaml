server:
        port: 8088
spring:
        datasource:
                url: jdbc:mysql://localhost:3300/demo?characterEncoding=utf-8&serverTimezone=UTC
                driver-class-name: com.mysql.cj.jdbc.Driver
                username: root
                password: gaosen

        data:
                redis:
                        host: localhost
                        port: 6379

mybatis-plus:
        mapper-locations: classpath:mapper/*Mapper.xml

